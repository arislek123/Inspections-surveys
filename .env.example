# Optional local environment settings for development.
# Firebase configuration is already included in src/firebase.ts for the deployed web app.
# Do not place passwords, private keys, or company secrets in this file.
